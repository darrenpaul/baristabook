export type GrinderCreateData = {
  name: string;
  notes: string;
  user_id: string;
};

export type GrinderResponseData = {
  created_at: string;
  id: string;
  name: string;
  notes: string;
  user_id: string;
};
